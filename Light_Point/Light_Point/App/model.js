function Shop(name, adress, mode, serialNumber)
{
    this.name = name;
    this.adress = adress;
    this.mode = mode;
    this.serialNumber = serialNumber;
}

function Product(name, desc)
{
    this.name = name;
    this.desc = desc;
}